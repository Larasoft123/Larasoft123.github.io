---
interface Props {
  Class?: string;
  text: string;
}

const { Class: className = "", text } = Astro.props;
---

<span
  class={`rounded bg-white/10 px-3 py-1 text-sm font-medium text-white ${className}`}
>
  {text}
</span>
