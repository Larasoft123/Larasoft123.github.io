---

interface Props {
  className?: string;
  id: string;
}

const { className,id} = Astro.props;


---


<section id={id} class={`mx-auto  max-w-4xl mt-16 p-8 md:p-6  lg:p-4 ${className}`}>
    <slot />
</section>