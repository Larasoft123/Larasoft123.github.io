---
import AWS from "@/assets/svg/aws.svg";

interface Props {
    shortIssuer: string;
}

const { shortIssuer } = Astro.props;


---

<!-- implement for all issuers an if with the correct logo -->
<!-- {
    shortIssuer === "SOMETHING" && <SOMETHING/>
} -->

<AWS/>